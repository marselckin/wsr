Burp Suite - анализ работы сайтов
Wireshark - анализ трафика
Zap - сканер уязвимостей web
nmap - сканер

установка python
python3 --version
su -
apt-get update && apt-get install python3
exit

volatility - анализ дамп

node js -Java
# Docker has specific installation instructions for each operating system.
# Please refer to the official documentation at https://docker.com/get-started/
# Pull the Node.js Docker image:
docker pull node:24-slim
# Create a Node.js container and start a Shell session:
docker run -it --rm --entrypoint sh node:24-slim
# Verify the Node.js version:
node -v # Should print "v24.20.0".
# Verify npm version:
npm -v # Should print "11.19.0".

Postman - анализ api
ghidra - реверс
sudo apt install ghidra
